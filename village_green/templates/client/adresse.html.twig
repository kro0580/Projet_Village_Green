{% extends 'base.html.twig' %}
{% block title %}
 Mes adresse
{% endblock %}
{% block body %}
 <div class="container">
  <a href="{{ path('add_adresse') }}" class="btn btn-info btn-sm float-right">Ajouter une adresse</a>
  <h1>Mes adresses</h1>
  {% if app.user.cliAdresses|length == 0 %}
   <p class="text-center">Vous n'avez pas encore ajouté d'adresse. Pour en ajouter une, veillez <a href="{{ path('add_adresse') }}">cliquer ici</a> </p>
  {% else %}
   <div class="row">
    {% for adresse in app.user.cliAdresses %}
     <div class="col">
         {% set formHtml %}
            {{ adresse }} <br>
         {% endset %}
         {{ adresse|replace({'[br]' : "<br/>"})|raw }}
     </div>
     <hr>
    {% endfor %}

   </div>
  {% endif %}
 </div>
{% endblock %}



