{% extends 'base.html.twig' %}

{% block title %}Valider ma commande{% endblock %}
{% block body %}
    <div class="container">
        <h1>Je passe ma commande</h1>
        <p>Choisissez vos préférences avant de passez votre commande</p>
        <hr>
        <div class="row">
            <div class="col-md-6">
                {% set formHtml %}
                    {{ form_start(form, {action:path('order_recap')}) }}
                    <div id="lien_adresse"><a href="{{ path('add_adresse') }}">Ajouter une adresse</a></div>
                    {{ form_label(form.adresseLiv, 'Mon adresse de livraison') }}
                    {{ form_end(form) }}
                {% endset %}
                {{ formHtml|replace({'[br]' : "<br/>"})|raw }}
            </div>
            <div id="recap_panier" style="margin-left: -40px;" class="col-md-6">
                <p><strong>Recapitulatif du panier</strong></p>
                <div class="table-responsive-md">
                    <table class="table table-hover text-center">
                        {% set total= null %}
                        {% for produit in cart %}
                            <tr >
                                <th><img style="height: 200px;" src="{{ asset('img/PRODUITS/')}}{{ produit.produit.proPhoto }}" alt="{{ produit.produit.proLib }}"></th>
                                <td>{{ produit.produit.proLib }} </td>
                                <td>x {{ produit.quantite }}</td>
                                <td>{{ produit.produit.proPrixAchat }}€</td>
                                <td>{{ (produit.produit.proPrixAchat * produit.quantite)|number_format(2) }}€</td>
                            </tr>
                        {% endfor %}
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
